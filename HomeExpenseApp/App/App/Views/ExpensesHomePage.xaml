<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App.Views.ExpensesHomePage"
             xmlns:vm="clr-namespace:App.ViewModels"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:ExpensesHomeViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#E8EAF6</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackLayout BackgroundColor="{StaticResource Accent}" VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
            <Label Padding="2" FontSize="Micro" HorizontalTextAlignment="Center" Text="Copyright 2022, All Rights Reserved By Varun Setia."/>
        </StackLayout>
        <ScrollView Grid.Row="1">
            <StackLayout Orientation="Vertical" Padding="30,24,30,24" Spacing="10">
                <Label Text="This month spent" FontSize="Title"/>
                <Label Text="Previous month spent" FontSize="Title"/>
                <Label Text="Future month forecast" FontSize="Title"/>

            </StackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
